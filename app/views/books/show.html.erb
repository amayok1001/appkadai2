<%= render '/books/list', { book: @book, user: @user} %>

<h2> Book detail</h2>
<table>
        <tr class="booksline">
        <th></th>
        <th></th>
        <th></th>
        </tr>
        <tr>
			<td><%= attachment_image_tag @user, :profile_image, :size =>"40x40", format:'jpeg', class: "pull-left profile-img", fallback: "no_image.png" %><br>
                <%= link_to user_path(@user) do %>
                <%= @user.name %>
                <% end %></td>
            <td><%= @bookf.title %></td>
            <td><%= @bookf.body %></td>
            <% if @user.id == current_user.id %>
            <td><%= link_to "Edit", edit_book_path(@bookf), class: "edit_book_#{@bookf.id} btn btn-success" %></td>
            <td><%= link_to "Destroy", book_path(@bookf), method: :delete , class: "destroy_book_#{@bookf.id} btn btn-danger", data: { confirm: 'Are you sure?' } %></td>
            <% end %>
        </tr>
</table>
